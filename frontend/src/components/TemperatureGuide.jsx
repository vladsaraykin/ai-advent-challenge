const rows = [
  ['0,0', 'Максимально предсказуемые и сфокусированные ответы', 'Классификация, извлечение данных, код, строгий JSON'],
  ['0,1–0,3', 'Очень стабильные ответы, минимум творчества', 'Бэкенд-интеграции, технические ответы'],
  ['0,4–0,7', 'Баланс точности и вариативности', 'Универсальные ассистенты, объяснения'],
  ['0,8–1,0', 'Больше разнообразия и креативности', 'Диалоги, мозговой штурм'],
  ['1,0–1,5', 'Довольно случайные ответы', 'Творческие тексты, генерация идей'],
  ['1,5–2,0', 'Очень непредсказуемые ответы', 'Эксперименты; обычно не для продакшена']
]

export default function TemperatureGuide() {
  return <section className="guide" aria-labelledby="guide-title">
    <div><h2 id="guide-title">Памятка по temperature</h2><p>Чем выше значение, тем свободнее модель выбирает продолжение.</p></div>
    <div className="table-wrap"><table><thead><tr><th>Temperature</th><th>Поведение</th><th>Подходит для</th></tr></thead>
      <tbody>{rows.map(row => <tr key={row[0]}>{row.map(cell => <td key={cell}>{cell}</td>)}</tr>)}</tbody></table></div>
  </section>
}
